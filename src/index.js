import React from 'react';
import ReactDOM from 'react-dom/client';
import Liverpool from './Liverpool'; //''는 기초 컴포넌트 설정

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Liverpool />
  </React.StrictMode>
); //Practice 부분 역시 자기자신이 설정.